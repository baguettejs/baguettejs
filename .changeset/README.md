# Changesets

Ajoutez un changeset pour chaque changement publiable :

```bash
bun run changeset
```

Puis préparez les versions et publiez depuis la racine :

```bash
bun run version-packages
bun run release
```
